# S3 server
cloud:
  aws:
    credentials:
      accessKey: accessKey
      secretKey: secretKey
    s3:
      bucket: modongmun
    region:
      static: static
    stack:
      auto: false

# EC2 warning
logging:
  level:
    com:
      amazonaws:
        util:
          EC2MetadataUtils: error
