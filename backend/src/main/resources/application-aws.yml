# S3 server
cloud:
  aws:
    credentials:
      accessKey: accesskey
      secretKey: secretkey
    s3:
      bucket: bucket setting name
    region:
      static: static
    stack:
      auto: false

# EC2 warning
logging:
  level:
    com:
      amazonaws:
        util:
          EC2MetadataUtils: error
